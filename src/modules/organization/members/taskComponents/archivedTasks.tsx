import { MemberTasksProps } from "types";

const ArchivedTasks = ({ tasks }: { tasks: MemberTasksProps }) => {
  return <div className="bg-red-500">ArchivedTasks</div>;
};

export default ArchivedTasks;
