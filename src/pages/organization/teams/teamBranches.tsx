import { TeamBranchesModal } from "components";
import { ModalProps } from "types";

const TeamBranches = (props: ModalProps) => {
  return <TeamBranchesModal {...props} />;
};

export { TeamBranches };
